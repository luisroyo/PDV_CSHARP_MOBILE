<Window x:Class="Pos.Desktop.Wpf.Views.ReportsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Pos.Desktop.Wpf.Converters"
        Title="Relatórios - PDV Multi-Vertical" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <local:StringToBoolConverter x:Key="StringToBoolConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <Style x:Key="ReportCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#34495E" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Relatórios e Análises" Style="{StaticResource HeaderTextStyle}" Foreground="White"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 Atualizar" 
                            Command="{Binding RefreshCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="❌ Fechar" 
                            Click="CloseButton_Click"
                            Background="#E74C3C"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Date Range Filter -->
                <Border Grid.Row="0" Style="{StaticResource ReportCardStyle}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Período de Análise" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Column="0" Text="De:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <DatePicker Grid.Column="1" SelectedDate="{Binding StartDate}" Margin="0,0,20,0"/>
                            
                            <TextBlock Grid.Column="2" Text="Até:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate}" Margin="0,0,20,0"/>
                            
                            <Button Grid.Column="4" 
                                    Content="📊 Gerar Relatórios" 
                                    Command="{Binding GenerateReportsCommand}"
                                    Background="#27AE60"
                                    Foreground="White"
                                    Style="{StaticResource ButtonStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Summary Cards -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Sales -->
                    <Border Grid.Column="0" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <TextBlock Text="💰" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Vendas Totais" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalSales, StringFormat=C}" FontSize="20" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Total Orders -->
                    <Border Grid.Column="1" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <TextBlock Text="📋" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Pedidos" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TotalOrders}" FontSize="20" FontWeight="Bold" Foreground="#3498DB" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Average Ticket -->
                    <Border Grid.Column="2" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🎫" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Ticket Médio" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding AverageTicket, StringFormat=C}" FontSize="20" FontWeight="Bold" Foreground="#9B59B6" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Top Product -->
                    <Border Grid.Column="3" Style="{StaticResource ReportCardStyle}">
                        <StackPanel>
                            <TextBlock Text="🏆" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Produto Top" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TopProduct}" FontSize="12" FontWeight="Bold" Foreground="#E74C3C" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Report Sections -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sales by Category -->
                    <Border Grid.Column="0" Style="{StaticResource ReportCardStyle}" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="📊 Vendas por Categoria" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                            <DataGrid ItemsSource="{Binding SalesByCategory}" 
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      AlternatingRowBackground="#F8F9FA">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Categoria" Binding="{Binding Category}" Width="*"/>
                                    <DataGridTextColumn Header="Vendas" Binding="{Binding Sales, StringFormat=C}" Width="100"/>
                                    <DataGridTextColumn Header="%" Binding="{Binding Percentage, StringFormat=F1}" Width="60"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- Top Products -->
                    <Border Grid.Column="1" Style="{StaticResource ReportCardStyle}" Margin="10,0,0,0">
                        <StackPanel>
                            <TextBlock Text="🏆 Produtos Mais Vendidos" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                            <DataGrid ItemsSource="{Binding TopProducts}" 
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      AlternatingRowBackground="#F8F9FA">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Produto" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="Qtd" Binding="{Binding Quantity}" Width="60"/>
                                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="📄 Relatório de Vendas" 
                            Command="{Binding ExportSalesReportCommand}"
                            Background="#27AE60"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="📊 Relatório de Produtos" 
                            Command="{Binding ExportProductsReportCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="👥 Relatório de Clientes" 
                            Command="{Binding ExportCustomersReportCommand}"
                            Background="#9B59B6"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="💰 Relatório Financeiro" 
                            Command="{Binding ExportFinancialReportCommand}"
                            Background="#E74C3C"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#34495E" Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="White" 
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
