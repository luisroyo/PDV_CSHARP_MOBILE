<Window x:Class="Pos.Desktop.Wpf.Views.CustomerManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Pos.Desktop.Wpf.Converters"
        Title="Gestão de Clientes - PDV Multi-Vertical" 
        Height="600" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <local:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <local:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
        
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#34495E" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Gestão de Clientes" Style="{StaticResource HeaderTextStyle}" Foreground="White"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ Novo Cliente" 
                            Command="{Binding NewCustomerCommand}"
                            Background="#27AE60"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="🔄 Atualizar" 
                            Command="{Binding RefreshCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="❌ Fechar" 
                            Click="CloseButton_Click"
                            Background="#E74C3C"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Customers List -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputStyle}"
                                 Name="SearchBox">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        
                        <Button Grid.Column="1" 
                                Content="🔍" 
                                Command="{Binding SearchCommand}"
                                Background="#3498DB"
                                Foreground="White"
                                Style="{StaticResource ButtonStyle}"/>
                    </Grid>

                    <!-- Customers DataGrid -->
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Customers}"
                              SelectedItem="{Binding SelectedCustomer}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              AlternatingRowBackground="#F8F9FA">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="CPF/CNPJ" Binding="{Binding Document}" Width="120"/>
                            <DataGridTextColumn Header="Telefone" Binding="{Binding Phone}" Width="120"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                            <DataGridCheckBoxColumn Header="Ativo" Binding="{Binding Active}" Width="60"/>
                            <DataGridTemplateColumn Header="Ações" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️" 
                                                    Command="{Binding DataContext.EditCustomerCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#F39C12"
                                                    Foreground="White"
                                                    Width="30" Height="25"
                                                    FontSize="10"
                                                    Margin="2"/>
                                            <Button Content="🗑️" 
                                                    Command="{Binding DataContext.DeleteCustomerCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#E74C3C"
                                                    Foreground="White"
                                                    Width="30" Height="25"
                                                    FontSize="10"
                                                    Margin="2"/>
                                        </StackPanel>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Customer Form -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Dados do Cliente" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,20"/>
                        
                        <!-- Nome -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Nome *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CustomerForm.Name}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Documento -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="CPF/CNPJ *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CustomerForm.Document}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Telefone -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Telefone" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CustomerForm.Phone}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Email -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Email" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CustomerForm.Email}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Endereço -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Endereço" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding CustomerForm.Address}" 
                                     Style="{StaticResource InputStyle}"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"/>
                        </StackPanel>

                        <!-- Data de Nascimento -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Data de Nascimento" FontWeight="Bold" Margin="0,0,0,5"/>
                            <DatePicker SelectedDate="{Binding CustomerForm.BirthDate}" 
                                        Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Ativo -->
                        <StackPanel Margin="0,0,0,20">
                            <CheckBox Content="Cliente Ativo" 
                                      IsChecked="{Binding CustomerForm.Active}"
                                      FontWeight="Bold"/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Vertical">
                            <Button Content="💾 Salvar" 
                                    Command="{Binding SaveCustomerCommand}"
                                    Background="#27AE60"
                                    Foreground="White"
                                    Style="{StaticResource ButtonStyle}"
                                    Height="40"/>
                            
                            <Button Content="🔄 Cancelar" 
                                    Command="{Binding CancelCommand}"
                                    Background="#95A5A6"
                                    Foreground="White"
                                    Style="{StaticResource ButtonStyle}"
                                    Height="40"/>
                        </StackPanel>

                        <!-- Error Message -->
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#E74C3C"
                                   FontSize="12"
                                   Margin="0,15,0,0"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}, ConverterParameter=Collapsed}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#34495E" Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="White" 
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
