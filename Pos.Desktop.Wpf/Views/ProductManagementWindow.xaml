<Window x:Class="Pos.Desktop.Wpf.Views.ProductManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Pos.Desktop.Wpf.Converters"
        Title="Gestão de Produtos - PDV Multi-Vertical" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <local:StringToBoolConverter x:Key="StringToBoolConverter"/>
        <local:InvertedBoolConverter x:Key="InvertedBoolConverter"/>
        
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
        
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#34495E" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Gestão de Produtos" Style="{StaticResource HeaderTextStyle}" Foreground="White"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ Novo Produto" 
                            Command="{Binding NewProductCommand}"
                            Background="#27AE60"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="🔄 Atualizar" 
                            Command="{Binding RefreshCommand}"
                            Background="#3498DB"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                    <Button Content="❌ Fechar" 
                            Click="CloseButton_Click"
                            Background="#E74C3C"
                            Foreground="White"
                            Style="{StaticResource ButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Products List -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search and Filters -->
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox Grid.Column="0" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource InputStyle}"
                                 Name="SearchBox">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        
                        <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding Categories}"
                                  SelectedItem="{Binding SelectedCategory}"
                                  Style="{StaticResource InputStyle}"
                                  Width="150"
                                  Margin="10,0,5,0"/>
                        
                        <Button Grid.Column="2" 
                                Content="🔍" 
                                Command="{Binding SearchCommand}"
                                Background="#3498DB"
                                Foreground="White"
                                Style="{StaticResource ButtonStyle}"/>
                    </Grid>

                    <!-- Products DataGrid -->
                    <DataGrid Grid.Row="1" 
                              ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              AlternatingRowBackground="#F8F9FA">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="100"/>
                            <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Categoria" Binding="{Binding Category}" Width="120"/>
                            <DataGridTextColumn Header="Preço" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                            <DataGridTextColumn Header="Estoque" Binding="{Binding Stock}" Width="80"/>
                            <DataGridCheckBoxColumn Header="Ativo" Binding="{Binding Active}" Width="60"/>
                            <DataGridTemplateColumn Header="Ações" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️" 
                                                    Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#F39C12"
                                                    Foreground="White"
                                                    Width="30" Height="25"
                                                    FontSize="10"
                                                    Margin="2"/>
                                            <Button Content="🗑️" 
                                                    Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Background="#E74C3C"
                                                    Foreground="White"
                                                    Width="30" Height="25"
                                                    FontSize="10"
                                                    Margin="2"/>
                                        </StackPanel>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Product Form -->
            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="15">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Dados do Produto" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,20"/>
                        
                        <!-- SKU -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="SKU *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Sku}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Nome -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Nome *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Name}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Descrição -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Descrição" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Description}" 
                                     Style="{StaticResource InputStyle}"
                                     Height="60"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"/>
                        </StackPanel>

                        <!-- Categoria -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Categoria *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Categories}"
                                      SelectedItem="{Binding ProductForm.Category}"
                                      Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Preço -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Preço *" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Price, StringFormat=F2}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Código de Barras -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Código de Barras" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Barcode}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Unidade -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Unidade" FontWeight="Bold" Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Units}"
                                      SelectedItem="{Binding ProductForm.Unit}"
                                      Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Estoque -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Estoque Inicial" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ProductForm.Stock, StringFormat=F0}" Style="{StaticResource InputStyle}"/>
                        </StackPanel>

                        <!-- Ativo -->
                        <StackPanel Margin="0,0,0,20">
                            <CheckBox Content="Produto Ativo" 
                                      IsChecked="{Binding ProductForm.Active}"
                                      FontWeight="Bold"/>
                        </StackPanel>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Vertical">
                            <Button Content="💾 Salvar" 
                                    Command="{Binding SaveProductCommand}"
                                    Background="#27AE60"
                                    Foreground="White"
                                    Style="{StaticResource ButtonStyle}"
                                    Height="40"/>
                            
                            <Button Content="🔄 Cancelar" 
                                    Command="{Binding CancelCommand}"
                                    Background="#95A5A6"
                                    Foreground="White"
                                    Style="{StaticResource ButtonStyle}"
                                    Height="40"/>
                        </StackPanel>

                        <!-- Error Message -->
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Foreground="#E74C3C"
                                   FontSize="12"
                                   Margin="0,15,0,0"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}, ConverterParameter=Collapsed}"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#34495E" Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="White" 
                       FontSize="12"
                       HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
